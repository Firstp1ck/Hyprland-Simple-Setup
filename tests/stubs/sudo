#!/usr/bin/env bash
set -euo pipefail
{
  printf 'sudo'
  printf ' %q' "$@"
  printf '\n'
} >> "$STUB_LOG"
while [[ $# -gt 0 ]]; do
  case "$1" in
    -n|-S|-k|-v) shift ;;
    *) break ;;
  esac
done
[[ $# -gt 0 ]] || exit 0
exec "$@"
